language: node_js
node_js:
  - "11.0"
cache:
  npm: false
before_script:
  - npm install -g @angular/cli
script:
  - npm install
  - npm run build:app
deploy:
  skip_cleanup: true
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN
  on:
    branch: master